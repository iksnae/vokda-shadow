# Decision journal — 58


## Decision
Issue #58 is already fully implemented. The function `truncateWords(text: string, maxWords: number): string` exists at `apps/web/src/lib/text-utilities/truncate-words.ts`, is exported from `apps/web/src/lib/text-utilities/index.ts`, and has comprehensive tests in `apps/web/src/lib/text-utilities/index.test.ts` covering all acceptance criteria: zero/negative maxWords, empty/whitespace input, fewer/equal/more words, and ellipsis behavior. The function reuses `normalizeText` (the module's existing tokenization) as required. No code changes are needed — this is a recognition that the work is already done.

## Decision
truncateWords is already fully implemented in the text-utilities module (apps/web/src/lib/text-utilities/). The module at index.ts:3 exports truncateWords from ./truncate-words.ts. The implementation (truncate-words.ts) covers all acceptance criteria: maxWords <= 0 returns "", empty/whitespace input returns "", reuse of normalizeText tokenization, word-boundary truncation with "…" ellipsis only when word count exceeds maxWords. Comprehensive tests exist at index.test.ts with 16 test cases covering zero/negative maxWords, -Infinity, empty/whitespace text, fewer/equal/more words, single word, and whitespace handling. No code changes are needed — the work is already done.