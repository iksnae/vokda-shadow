# Decision journal — 35


## Decision
The fix is isolated to `getTagDef()` in `apps/web/src/lib/ssml/tags.ts` line ~206: change `t.tag === tagName` to a case-insensitive comparison (`t.tag.toLowerCase() === tagName.toLowerCase()`). No callers outside the file need changes — `isTagSupportedByProvider` calls `getTagDef` internally, and `validate.ts` passes tagNames from XML DOM (which preserves case from source). Tests go in `tags.test.ts` covering uppercase (`getTagDef('PROSODY')`) and mixed-case (`getTagDef('Break')`) lookups, plus the `isTagSupportedByProvider` cascade.

## Decision
Verified codebase: `getTagDef()` at tags.ts:206 uses `t.tag === tagName` (case-sensitive). All tag definitions in SSML_TAGS use lowercase names. Fix is a single-line change to `t.tag.toLowerCase() === tagName.toLowerCase()`. Confirmed callers: `isTagSupportedByProvider()` (tags.ts:211), `validate.ts:104` via `getTagDef(tagName)`, and `validate.ts:99` via `KNOWN_TAGS.has(tagName)` — but the KNOWN_TAGS check is a separate path not in scope for this issue. Test framework: vitest. Verification command: `npm run test` (runs `vitest run` in apps/web).