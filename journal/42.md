# Decision journal — 42


## Decision
Cross-link targets for the glossary: (1) "Provider ID" cross-links to `docs/PROVIDER_IDS.md` — this file does NOT exist yet; the glossary entry should link to it forward-looking, and PROVIDER_IDS.md can be created later. (2) "SSML tag" cross-links to the SSML docs — no standalone SSML doc exists; the best current target is `docs/DISCOVERY.md#ssml-editor` (the SSML Editor section). The glossary can link there. (3) The README.md "Product Docs" list is the natural linking point for GLOSSARY.md.

## Decision
Cross-link targets for GLOSSARY.md:
- "SSML docs" → `docs/DISCOVERY.md#ssml-editor` (the authoritative SSML documentation in this repo; DISCOVERY.md has the SSML Editor section with tag toolbar, validation, quick reference, and provider matrix)
- "Provider ID" → `docs/PROVIDER_IDS.md` does NOT currently exist. The glossary should still cross-link to it per the issue's instruction, since PROVIDER_IDS.md is anticipated. Meanwhile, provider IDs are documented in `docs/SYNTHESIS_API.md#supported-providers` and `docs/SCHEMA.md`.
- The glossary link in README.md goes under the "Product Docs" bullet list alongside the other doc entries.