# Decision journal — 50


## Decision
The composition is: normalizeText FIRST, then chunkText on the normalized string, then map each chunk through wordCount. The empty/whitespace path (→ []) and the maxLength≤0 path (→ throw) are both naturally satisfied by delegating to the existing functions — no special-case branching in prepareForSynthesis itself. wordCount internally normalizes again on already-normalized text, which is a benign no-op. The new module goes at apps/web/src/lib/prepare-for-synthesis.ts alongside its test file apps/web/src/lib/prepare-for-synthesis.test.ts. No barrel/index update is needed per the issue scope.

## Decision
Plan for #50 confirmed. New module at `apps/web/src/lib/prepare-for-synthesis.ts` with co-located test at `apps/web/src/lib/prepare-for-synthesis.test.ts`. Will import `chunkText` from `./chunk-text.js` and `normalizeText`/`wordCount` from `./text-utilities/index.js`. `prepareForSynthesis` normalizes first with `normalizeText` (collapses whitespace → trims), then delegates to `chunkText` for splitting. Since `chunkText` trims itself, the double-trim is a no-op after `normalizeText` already trimmed. For empty/whitespace input, `normalizeText` returns `""` → `chunkText("")` returns `[]` → early return `[]`. `maxLength <= 0` is handled by `chunkText`'s own guard (throws). No barrel exports to update — the issue doesn't require it, and the module is a standalone composition helper.