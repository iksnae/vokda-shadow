# Decision journal — 54


## Decision
New file `apps/web/src/lib/text-utilities/estimate-reading-time.ts` — a pure function `estimateReadingTime(text, wordsPerMinute?)` that delegates to the existing `wordCount(text)`, then divides by `wordsPerMinute` (default 200) and returns `Math.ceil(count / wpm)`, floored to 0 for empty input. Export it from the barrel `index.ts`. All tests go in the existing `index.test.ts` under a new `describe('estimateReadingTime', ...)` block — because the existing pattern puts every export's tests in that single file (no per-function test file).