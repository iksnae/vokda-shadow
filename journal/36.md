# Decision journal — 36


## Decision
Product lead confirms: `getAttrDef(tagName, attrName): SsmlAttrDef | undefined` — add to `tags.ts`, reuse `getTagDef` for case-insensitive tag resolution, then case-insensitive `.find` on `def.attributes`. Return `undefined` for unknown tag OR unknown attribute. Test in `tags.test.ts` with vitest: 4 cases (known attr, case-insensitive match, unknown tag, known tag + unknown attr). No changes outside `tags.ts` + `tags.test.ts`.