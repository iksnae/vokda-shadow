# Decision journal — 48


## Decision
The text-utilities module will live at apps/web/src/lib/text-utilities/ as a folder with three function files (normalizeText.ts, wordCount.ts, truncateWords.ts), a barrel index.ts, and a single test file. This follows the established pattern where chunk-text.ts co-locates with its test at the same level. The new module is a folder (not a flat file) because the issue explicitly calls for "each in its own file" plus a barrel, which implies a directory grouping them.

## Decision
Files will live at `apps/web/src/lib/text-utilities/` as a new subdirectory, following the existing `lib` co-location pattern (source + tests side-by-side). Layout: `normalize-text.ts`, `word-count.ts`, `truncate-words.ts`, `index.ts` (barrel), and a single test file `text-utilities.test.ts`. Naming follows kebab-case convention already used by `chunk-text.ts`, `language-utils.ts`, etc.

## Decision
Each function follows the existing `chunk-text.ts` pattern: pure function, no imports, JSDoc with @param/@returns/@example, single named export. The barrel index re-exports all three from one entry point. Tests follow the existing vitest pattern (describe/it/expect from 'vitest', no describe nesting needed per function since the suite is small).

## Decision
Module will live at `apps/web/src/lib/text-utils/` with individual files (`normalizeText.ts`, `wordCount.ts`, `truncateWords.ts`), a barrel `index.ts`, and a single test file `text-utils.test.ts` — following the project's co-located test convention (e.g. `chunk-text.test.ts` next to `chunk-text.ts`). Vitest glob `src/**/*.test.ts` already covers this. No existing barrel patterns in the repo, but the issue explicitly requires one.