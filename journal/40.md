# Decision journal — 40


## Decision
Closed #25 (build non-convergence, parked) and replaced it with #40 — a tightly scoped issue for just `docs/PROVIDER_IDS.md`. Stripped out the version-stamp half (already satisfied by `generatedAt` + `total` on providers.json) and the bonus reconciliation ask. This is a pure docs deliverable: a stable-contract page for integrators with a deprecation policy.