# Decision journal — 40


## Decision
Closed #25 (build non-convergence, parked) and replaced it with #40 — a tightly scoped issue for just `docs/PROVIDER_IDS.md`. Stripped out the version-stamp half (already satisfied by `generatedAt` + `total` on providers.json) and the bonus reconciliation ask. This is a pure docs deliverable: a stable-contract page for integrators with a deprecation policy.

## Decision
Issue #40 plan: (1) Edit existing docs/PROVIDER_IDS.md — remove `huggingface` row (not in providers.json), add integrator-friendly preamble, soften normative language. (2) Link from README.md Product Docs list. (3) Optionally link from docs/API.md. No new files needed, no code changes. The doc already covers stable contract, deprecation policy, and changelog expectation.